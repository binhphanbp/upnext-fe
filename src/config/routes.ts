export const routes = {
  home: "/",
  jobs: "/jobs",
  login: "/login",
  register: "/register",
  dashboard: "/dashboard",
  dashboardJobs: "/dashboard/jobs",
  dashboardApplications: "/dashboard/applications",
} as const;
