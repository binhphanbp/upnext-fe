import { AdminDashboard } from "@/features/admin/components/AdminDashboard";

export const metadata = {
  title: "Admin Dashboard | UpNext",
};

export default function AdminPage() {
  return <AdminDashboard />;
}
